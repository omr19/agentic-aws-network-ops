# VPC Peering Module

Replaceable interconnect boundary for same-Region peering, cross-VPC DNS resolution, and
the approved bidirectional private routes. It owns neither VPCs nor workloads, allowing
the later TGW configuration to replace this layer without repackaging those modules.
