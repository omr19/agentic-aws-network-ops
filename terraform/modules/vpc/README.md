# VPC Module

Reusable boundary for one approved private VPC, its two private subnets, dedicated route
tables, healthy-baseline NACL, DNS settings, and optional controlled-session Flow Logs.
It deliberately creates no Internet Gateway, NAT Gateway, public subnet, endpoint, or
workload.
